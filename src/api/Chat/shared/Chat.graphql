type Chat {
    id: Int!
    messages: [Message]
    passengerId: Int!
    passenger: User!
    driverId: Int
    driver: User!
    ride: Ride!
    rideId: Int
    createdAt: String!
    updatedAt: String       
}